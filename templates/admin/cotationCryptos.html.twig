{% extends 'base.html.twig' %}

{% block title %} Cotation Cryptos List {% endblock %}

{% block body %}
    <div class="container cryptos">
         <h1>Les Crypto - monnaies</h1>
        <div class="row">
            {% for cotationCrypto in cotationCryptos %}
            <div class="col-md-4">
                {# {% set url= path('admin_cotationCryptoShow',{'id':cotationCrypto.id}) %} #}
                <a href=""><img src="{{ cotationCrypto.image }}" alt=""></a>
                <h2><a href="">{{ cotationCrypto.nom}}</a></h2>
                
                <p><a href="" class="btn btn-primary">Voir Cours  </a></p>
            </div>
            {% endfor %}
        </div> 
    </div>
{% endblock %}